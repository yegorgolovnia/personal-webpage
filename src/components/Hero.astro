---

---

<section class="hero" id="hero-section">
    <div class="content">
        <div class="hello-text pixel-text fade-in">hi, I am</div>
        <h1 class="fade-in">YEGOR GOLOVNIA</h1>

        <div class="intro fade-in-delay">
            <p>
                I'm an operations generalist and builder with 10+ years of
                experience bridging the gap between raw vision and scalable
                systems.
            </p>
            <p>
                My expertise covers everything from ecosystem growth and
                orchestrating massive tech events, to high-impact capital
                allocation.
            </p>
            <p>
                Having spent years in the Web3 space scaling DAOs, my current
                focus is building custom agentic AI pipelines that multiply
                teams' output. Alongside my AI work, I remain deeply involved in
                public goods funding, leading QF at <strong>Giveth</strong> and actively
                contributing to <strong>TheDAO Security Fund</strong>.
            </p>
            <div
                class="cta-container fade-in-more-delay"
                style="margin-top: 2rem;"
            >
                <p style="margin-bottom: 1rem;">
                    Available for fractional ops and custom AI automations.
                </p>
                <a
                    href="#contact"
                    class="cta-button"
                    onclick="event.preventDefault(); document.querySelector('#contact').scrollIntoView({ behavior: 'smooth' });"
                >
                    Start a conversation <span
                        style="font-family: var(--font-sans); margin-left: 0.2rem;"
                        >→</span
                    >
                </a>
            </div>
        </div>
    </div>

    <style>
        .hero {
            min-height: 50vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            position: relative;
            overflow: hidden;
            border-bottom: 1px dashed var(--color-accent-subtle);
            margin-bottom: 2rem;
            padding-top: 6rem; /* Increased margin above "hi, I am" */
        }

        .content {
            position: relative;
            z-index: 2;
            max-width: 900px;
            pointer-events: none; /* Let mouse events reach the section behind it for the canvas effect */
        }

        .content > * {
            pointer-events: auto; /* Re-enable pointer events for text/buttons inside content */
        }

        .sys-msg {
            margin-bottom: 1rem;
            opacity: 0.7;
        }

        .hello-text {
            font-size: 1.2rem;
            color: var(--color-text-muted);
            margin-bottom: 0.5rem;
            text-transform: lowercase;
            opacity: 0.8;
        }

        h1 {
            font-family: "Geist Pixel Circle", sans-serif;
            font-size: clamp(3rem, 10vw, 6rem);
            line-height: 1;
            margin-bottom: 1rem;
            letter-spacing: -0.02em;
            color: var(--color-text);
            text-shadow: 2px 2px 0px var(--color-accent-subtle);
        }

        .subtitle {
            font-size: 1.2rem;
            color: var(--color-accent);
            margin-bottom: 2rem;
        }

        .pixel-text {
            font-family: var(--font-pixel);
        }

        .color-accent {
            color: var(--color-accent);
        }

        .intro {
            font-size: 1.2rem;
            max-width: 600px;
            line-height: 1.6;
            color: var(--color-text);
            opacity: 0.9;
            backdrop-filter: blur(2px);
        }

        .intro p {
            margin-bottom: 1rem;
        }

        .intro p:last-child {
            margin-bottom: 0;
        }

        /* Entry Animations */
        .fade-in {
            animation: fadeInUp 0.8s ease forwards;
            opacity: 0;
            transform: translateY(10px);
        }

        .fade-in-delay {
            animation: fadeInUp 0.8s ease 0.2s forwards;
            opacity: 0;
            transform: translateY(10px);
        }

        .fade-in-more-delay {
            animation: fadeInUp 0.8s ease 0.4s forwards;
            opacity: 0;
            transform: translateY(10px);
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .cta-button {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.8rem 1.5rem;
            font-family: var(--font-pixel);
            font-size: 1.1rem;
            color: var(--color-accent);
            border: 1px dashed var(--color-accent-subtle);
            background: rgba(0, 255, 65, 0.02);
            border-radius: 4px;
            text-decoration: none;
            transition: all 0.2s ease;
        }

        .cta-button:hover {
            border-color: var(--color-accent);
            background: rgba(0, 255, 65, 0.05);
            opacity: 1; /* Override default link hover opacity jump */
        }

        @media (prefers-reduced-motion: reduce) {
            .fade-in,
            .fade-in-delay,
            .fade-in-more-delay {
                animation: none !important;
                opacity: 1;
                transform: none;
            }
        }
    </style>
</section>
